# 经验沉淀(lessons)—— Claude /loop 编排器每轮诊断后追加

> 由方式 B 的编排器 Claude 维护;方式 A(OS cron)不维护。
> 格式:`- [日期] [现象] → [处理/教训]`。只记确实复用的经验,别灌水。

- (暂无)
- [2026-08-12] DeepSeek 生成调用偶发 ReadTimeout(60s)→ 直接崩整轮 → 已在 `llm/mechanisms.generate_expression` 给 `provider.complete` 加 try/except + 重试 + `random_tree` 兜底;单次 LLM 超时/连接错不再崩轮(回测异常本就有 try/except 跳过)。
- [2026-08-12] `cache/alphalab_in` + `cache/alphalab_out` 每个回测候选留 ~80MB 结果目录(report.html+factor_aligned.parquet),不清理会爆盘(13 轮累积 ~16GB)→ 已在 `AlphalabEvaluator.evaluate` 改为解析完即删结果目录+输入 parquet(`keep_output=False` 默认);指标已进 checkpoint,原始结果用完即弃。要留某因子完整 report.html 可 `keep_output=True` 或重跑。
- [2026-08-12] 尝试 ThreadPool 并行回测(--workers 4)无收益(11 回测 13.7min ≈ 串行):alphalab 内部已多核(~2.5 核/个),4 并发把 12 核占满→每个变慢;且含 rank_ts 的表达式求值是 GIL 绑定 pandas,线程并行不了 → **回测默认改回串行(--workers 1)**;提效改用「串行 + 加大 n」(n=100 hourly≈37min/轮,候选/小时×3)。多核富余机器可再试 workers=2~3。
- [2026-08-12] 探索模式(LLM 占比高)下 DeepSeek 偶发生成 depth>4 的表达式,触发 `evolve.generate` 的硬 `assert` 崩轮 → 改为「非法/超深候选跳过 + random_tree 补足」,单个坏候选不再崩轮。
- [2026-08-14] 8-12 记的「解析完即删」其实没修干净:`AlphalabEvaluator.evaluate` 的清理写在 `parse_dir` 成功之后,alphalab 异常/超时/无产出会提前 raise,失败候选照样漏删输入 parquet+结果目录(累积 395 in + 395 out ≈ 几 GB)→ 已把清理挪进 `try/finally` 保证异常也删,并一次性清掉残留。教训:清理务必放 finally,别只写在成功路径。
- [2026-08-14] alphalab 升级 0.4.2(自 0.3.0)→ config 删 `ic.nw_lag`/`group.long_excess` 两行,分年 csv 的年份列从 `date` 变无名索引列(改 `index_col=0` 读);单次回测 ~37s(旧 ~90-110s,约 3x 提速)。升级后必须重跑 `alphalab init`(缓存结构变)。
- [2026-08-14] 连续 20 轮 0 入库曾被误判成「搜索饱和」,真因是 alphalab 升级后 config 不兼容、每次回测抛 ValidationError 被 try/except 吞成「回测异常」→ 教训:诊断长期 0 入库先看回测是真跑(test_hashes 在涨)还是全异常;现 run_round_cli 会打印 REJECTS 样本。
- [2026-08-14] `run_round` 原在回测前就 `add_tested`,回测异常也标已测 → 产生不可重试的「幽灵候选」(被拒候选表达式不落盘,只留 hash)→ 改为审查未过/回测成功才标已测、回测异常不标,轮内去重用内存集。
- [2026-08-14] DeepSeek 生成偶发超时会把整轮生成拖到几十分钟(60s 超时 × 3 重试 × ~10 LLM 候选,且 stuck 预算把 30% 分给 LLM 放大)→ LLM 超时 60→30s、重试 3→2、`Evolver.generate` 加 120s 总时间预算(超预算降级随机);`review_expression` 加 try/except(失败放行不崩)。
- [2026-08-14] alphalab 升 0.4.2 后单次回测降到 ~37s(0.3.0 约 90-110s),重测并行:workers 1/2/3/4/6 → 1.00/1.89/2.63/2.71/4.45x,但 3→4 无增益(超订 6 物理核)、6 路掉 1 个回测 → **甜点 workers=3**。8-12 那条「并行无收益→串行」是 0.3.0 的结论,已随升级作废。
- [2026-08-14] 确定性坏因子拦截:`roc` 对近零字段(如 overnight 隔夜收益)做变化率,分母接近 0 爆 Inf → zscore 截面传染成全 NaN,alphalab 必拒 → 在 `AlphalabEvaluator.evaluate` 回测前查有限值占比,<1% 直接抛错省 ~37s/个。零误伤(全 NaN 因子本就永远过不了);放在真实 evaluator 而非基类,避免 mock 小面板假阳性。
- [2026-08-15] `export_factors.py` 只写不删,「保优淘劣」替换因子会换 hash → 旧 parquet 残留(曾 54 文件 vs 42 因子)→ 导出后按 manifest 自动清掉不在清单里的旧 parquet。
- [2026-08-15] 因子库过度收敛到 log_mv/log_amount(流动性/规模,32+20 次),overnight/影线/ret 等其它机制族字段几乎没挖 → `Evolver` 加字段使用频率引导:变异「换信号源」按 1/(1+使用次数) 加权,偏向未充分挖掘字段,打破一家独大。
- [2026-08-16] 「保优淘劣」替换逻辑缺陷:filter #9 只替换「最相关的 1 个」旧因子,新因子与其它旧因子仍 ≥0.70 → 库积累出 415 对高相关(占 22.7%)→ 改为「综合分全面优于所有相关旧因子才替换全部」,并对现有库贪心去重(62→20,两两 <0.70)。教训:相关性约束是全局不变量,替换要覆盖所有相关因子,不能只盯最相关那个。
- [2026-08-16] 生成端接收益来源反馈:`generate_expression` 加 field_usage 参数,把字段使用频率映射到 12 机制族 coverage,`pick_mechanism` 偏向未充分挖掘的机制(M5 的「优先未覆盖族」终于接上);`Evolver.llm_op` 透传 `self._field_usage`。
- [2026-08-17] 21 类收益来源标签里 12 类「跳空」类零覆盖(库内无 overnight 因子),仅靠 1/(1+count) 反使用偏置不够 → 升级为**boost 乘数优先跳空**:`Evolver._boost_fields`(overnight 4x / intraday 2x / amplitude 2x)+ `random_tree` 支持 `field_weights` 非均匀叶子选择 + `mechanisms.MECHANISM_BOOST`(ts_overnight_gap 4x 等)。实测 overnight 字段出现率 7.7%→26%、ts_overnight_gap 机制族 8.3%→22.4%,log_mv/log_amount 被压到 ~0.3%。注意:boost 要同时打在「字段选择(random/mutate)」和「机制族选择(LLM)」两层,只改一端无效。
- [2026-08-17] 智谱 key 调 `/api/paas/v4` 报 1113「余额不足」但 key 本身有效 → 该 key 是 **Coding Plan 订阅**,必须走 `/api/anthropic`(Anthropic Messages 格式,与 Claude Code 同端点),两套端点计费独立互不相通。已加 `AnthropicMessagesProvider` + `glm-anthropic` preset,`settings._build("glm")` 路由到该端点(超时 120s、max_tokens 4096,GLM_TIMEOUT/GLM_MAX_TOKENS 可调)。若 429 限流,generate/review 已有 random_tree 兜底与放行兜底,不会崩轮。
- [2026-08-17] 用户引研报 §16/17 质疑 `max(min(·,120),5)`(120日滚动最小→5日滚动最大,非数值截断)与 `std(std(·))` 入库:审查端原本只查边界/量纲,无「过度平滑」类;FSA 只对整树骨架去重,半树相同可绕过(右半树 `zscore(max(min(FLD,N),N))` 曾 6/34)→ 加审查过滤5(平滑嵌平滑 / 极值嵌极值直接拒,回测前拦,省 ~37s/个)+ 过滤15(≥4 节点同构子树在库存出现 ≥2:候选质量全面更优 ×1.05 → 保优淘劣替换该族全部旧因子,否则拒;存量豁免——用户指出「直接拒」会误杀同族更优挑战者,故带替换,复用 #9 replace_hashes 管线)。另注意:回测窗口 (2018,2025),库存对 2026 风格零外样本——窗口滚动 + 全库复检(动作4)待做。
- [2026-08-17] 按 2026-08-17 结构先验清洗存量库(`clean_library.py`,34→16):9 个结构违规(6 极值嵌套 + 3 过度平滑)+ 9 个家族裁剪(`rank_cs(std(rank_cs(FLD),N))` 8→2、`zscore(std(rank_cs(FLD),N))` 3→2、`add(roc(rank_cs(FLD),N),std(rank_cs(FLD),N))` 3→2;族内按 _quality 综合分保前 2)。FSA 整树计数同步扣减;export 重建并清 29 个残留 parquet;备份 `checkpoint.json.bak-20260817-preclean`、台账 `library_clean_20260817.json`。被移除因子的 hash 仍在 tested_hashes,同表达式不会僵尸复活。
- [2026-08-17] LLM 审查精判(`review_expression`)自项目启动以来从未接入轮次管线——「审查」一直是确定性过滤,REVIEW_PROVIDER 配置形同虚设(smoke 测试能通但轮内不消费)→ 接线为「过滤16 LLM 终审」:仅对过全部过滤、即将入库的候选(每轮 1-2 个)把关,LLM 不可用放行不崩;同时 mechanisms 加 _bump 计数,每轮 STATUS 前打 LLM 健康行(生成 ok/解析失败/API错/兜底 | 终审 ok/错),静默兜底从此可见。
- [2026-08-17] 用户发现导出因子 0007 于 2019-04~06 覆盖率塌陷(05 月 0%)→ 三层根因:①log_mv 在 2019-04-18/19 出现 0 值,pct_change 除零产生 ±inf;②`op_roc` 未像 `op_div` 那样消毒 inf,毒化截面 zscore 的 mean/std → 当天整截面 NaN;③外层 `max(·,40)` 的 min_periods=40「窗口内全有效才出值」把 2 个坏日放大成 40 个交易日的全市场瘫痪(loop.log 里长期存在的 RuntimeWarning「invalid value encountered in subtract」即此签名)→ 修复:op_roc 输出 ±inf→NaN,修复后该因子 2019 覆盖 35/0/23%→~61%。库存 18 个中含 roc 的 6 个里 5 个是 roc(rank_cs(·)) 形态(输入∈(0,1] 无除零)不受影响,仅 roc(原始字段) 有此风险。嵌套 ts 算子 min_periods=n 的 NaN 乘性放大是通用结构风险,「月覆盖率下限过滤」列为候选防线待拍板。
- [2026-08-17] 算子库审计(0007 事故延伸):13 个原始面板实测**零 inf**——±inf 全部由 div/roc 除零「生成」;分母类字段零值率高(overnight 10.3%/up_shadow 8.6%/down_shadow 7.6%)→ 落地三层消毒:叶子求值入口 ±inf→NaN(`expression.evaluate`)、`op_zscore` 入口消毒(防单个 inf 毒化整截面)、`op_roc` 修齐 `op_div` 口径;新增「覆盖率防线」(回测前确定性:任一月覆盖 < 前后各 12 个月中位 ×0.6 → 拒并永久去重,防嵌套 ts 算子 NaN 乘性放大);审查 prompt 补算子语义(时序算子第二参=滚动窗口非数值截断,否则 glm-5.3 会把 max(x,40) 误读为 clip 而错杀)。复检 0007:修复后指标全面改善(ic_series 1901→1942 天)但与库内第一名 IC 相关 0.78、综合分不足以替换 → 依现行规则属冗余因子。
- [2026-08-17] 全库体检(`health_check.py`,结构/覆盖/家族/相关/阈值/LLM 终审)暴露两件事:①覆盖率防线未切回测窗口,把 2015 warmup 月当塌陷 → **会让所有候选被误杀**(单测没覆盖 warmup 场景;修复:只看 BACKTEST_START 起,warmup 用例锁定)——体检工具在部署前抓到了它;②LLM 终审③类比确定性规则宽得多(拒库内 11/17,含第一名 `std(rank_cs(·))`"缺乏经济含义"),且对近似形态标准不稳 → 收窄 prompt 与代码规则严格同义(仅「直接子节点同类嵌套」算,其它堆叠明示不得据此拒);实测三个误拒翻正、div 除零与真极值嵌套仍被正确拒。另:#13 因 2020-07 一字板潮致 `min(amplitude,120)` 分母归零、单月覆盖 23%(用户拍板保留观察)。
- [2026-08-17] 用户发现 skew 因子整体覆盖仅 ~54% → **pandas `rolling().skew()` 有幽灵 NaN bug**:窗口完整、方差非零仍返回 NaN(2018-2025 adj_close 面板 53 万格;同 pandas 的非滚动 `Series.skew()` 正常)→ `op_skew` 改用滚动一/二/三阶矩和自实现(调整 Fisher-Pearson G1,与 Series.skew 同口径),覆盖率 54%→78.9%,抽查 271/271 精确一致(<1e-8);min_periods 窗口纪律只是次要因素(±2pp)。**教训:①慢性低覆盖是覆盖率防线的盲区——防线只抓「相对本地中位的悬崖」,均匀的低覆盖(54%)完全通过,库内 5/15 含 skew 因子的指标都是带伤算的;②pandas 滚动统计量在关键路径上要先小样本对拍再用**。
- [2026-08-17] IS/OOS 切分落地事故与修复:①alphalab 强制因子宽表覆盖其 yaml `sample` 区间的**每个交易日**——parquet 切 IS 窗口而 yaml 仍是全窗口 → 全部候选被拒(`缺 609/1942 个交易日`);②重准入脚本把「回测异常」当「保守移除」→ **基础设施错误瞬间清空整库(15→0)**。修复:适配层按窗口自动派生 yaml(`_config_for_window`,sample.start/end 替换+缓存);重建脚本铁律「回测/求值异常→跳过报告,绝不移除」,且批量操作前备份 checkpoint。恢复:manifest.csv 兜底完整恢复表达式清单。**教训:①批量治理脚本必须区分『因子不合格』与『基础设施故障』;②checkpoint 的 git 提交不能只靠每日备份——大操作前先 commit(这次靠 manifest 兜底纯属幸运)。**
- [2026-08-17] IS/OOS 样本切分正式上线(用户拍板):IS 2018-01-01~2023-06-30(全部筛选/入库)/ 隔离带 2023-07~2024-12(18 个月 ≥ 最深窗口链 310 交易日,只作 OOS warmup)/ OOS 2025 全年(入库时自动回测一次存 `oos_metrics`,lib_status 展示 IS→OOS 衰减)。铁律:OOS 绝不回流生成/筛选端(否则样本外烧掉)。BACKTEST_YEARS 改 (2018,2023)。存量按 IS 规则重新准入。
- [2026-08-18] IS-ONLY 三项收口(用户拍板):①tested_hashes 不释放重测(历史多为低效组合);②OOS 一年(2025)足够;③**OOS 系统性崩塌跳闸**——每轮 STATUS 后自动「OOS体检」(带 OOS 因子 ≥5 个时:中位 OOS IC≤0 或负占比 ≥50% → OOS ALERT),编排器见 ALERT 立即停止续链并向用户汇报,等待人工决策。
- [2026-08-18] 终审拒因回流对演化候选失效(轮 382 首个终审拒:roc 作用于值域 [0,1] 的截面排名,近零排名产生爆炸性比值——结构类拒因本应回流,但 hash→族登记只在 LLM 生成时做,变异/交叉候选无族归属)→ 补链:①入库记录 `family` 字段(LLM 候选生成时登记);②`Evolver.generate` 输出与候选对齐的 `last_gen_meta`(op+父本 hash);③编排层让演化子代继承父本族并 `register_family`——拒因回流自此覆盖全部候选路径。另:LLM 生成时间预算 120s→360s(glm-5.3 思考延迟 + 单次挂死曾耗尽预算致 LLM 分支静默降级,健康行 ok2 暴露后修复)。
- [2026-08-18] 用户指出 0001 因子数值错配 → 全库实测:15 个二元顶层因子中 **8 个分支尺度比 ≥3.7x(最重 33.6x)**,根因不是 zscore-vs-rank(0001 仅 1.9x,全库最平衡之列)而是 **`roc(rank_cs(·))`**:roc 做除法、rank 有 [0,1] 下界趋零 → 爆炸性比值(分支 std 达 9.6);alphalab 的 winsorize+standardize 让评测仍公平,但导出原始值被爆炸支支配。用户拍板「**占大头直接拿分支**」而非拒绝 → 落地:①roc 语义闸(直接子节点为 rank_cs/zscore → 拒,数学不良定义);②**分支支配简化**(顶层 add/sub 分支截面 std 比 ≥3x → 取支配支,树少一层信号不变,简化后深度不足/与已测重复则出局;平衡时直接合成面板免二次求值,零额外成本);③存量 8 个失衡因子**祖父化**,由保优淘劣自然换代(health_check 会按新规则标记它们,属预期)。
- [2026-08-18] **adj 口径二修(全链路零未来函数收官)**:用户审计延伸——adj_*=raw×f_t/**L** 的 L 取数据末尾(含未来公司行动),比率类用法精确相消但 delta/价差/水平类运算把逐股 1/L 带入截面 → 改为 `raw×f_t`(纯 PIT 累计复权),exlatest 连接整体删除。验证:10 个派生字段**逐值一致**(相消性实测确认);档案刷新 15 因子:**13 个逐位不变**、delta(adj_high) 因子 IC +9.3%/OOS +17.9%(未来锚原在拖累)、max(adj_low,5) 因子 IS IC **−20.6%**(真实能力被未来信息虚增,审计文档点名的项)。至此:mv 时点 + adj 纯 PIT + 派生相消 + 算子全后视 + 连接全 ASOF——**引擎全链路零未来函数**。
- [2026-08-24] LLM 生成解析失败率 20%(探针 15 次实测复现)→ 根因:生成 prompt 只写"时序算子带窗口 n"但未说明必填与范围,GLM 产出 rank_ts(x) 缺参(2例)和窗口 240 超界 [5,120](1例)→ prompt 硬规则补「时序算子必须带第二参数窗口 n 且 5≤n≤120」后实测 7%(历史均值),非模型退化。
- [2026-08-24] loop.log 断更(停在 8/19)→ run_round_cli 只写 stdout,历史由 PowerShell `*>> output/loop.log` 重定向落盘 → 链式跑轮须带 `>> output/loop.log 2>&1`,否则上轮 SIGNALS/LLM健康行无从回查。
- [2026-08-24] 失败模式库上线(用户需求):死证据口径——**纯 #9 IC相关/#15 同构家族拒不算死证据**(那是库内在位强因子占位,属保优淘劣挑战者路径,回流规避会杀死换代机制;实测纯占位拒 1654 条/72 骨架被正确排除),review 结构拒/回测异常/规则 1-8/12-14/16 才算;回流=LLM prompt 注入全灭骨架 Top12 + 演化候选重采样(每槽≤2 次)。另:mock 轮会污染真实 checkpoint 的 tested_hashes(--force 跑的 mock 测试后须从 git 恢复 checkpoint)。
- [2026-08-24] 双口径相关性上线(借鉴 wq-alpha-research 实证:相关必须算日收益而非累计曲线,IC 相关≠收益相关):PnL 口径(多空日收益 ls_ret)只进体检行不做准入(用户拍板);存量 23 因子回填实测——IC 口径 253 对 0 违规/105 灰区,但 PnL 口径 **38 对 ≥0.7**(Top 0.852)+111 灰区:IC 口径一直蒙着共同暴露的盲区,单一价量数据源的相关天花板已被触及(wq 同结论:同池因子日收益相关普遍 0.3-0.6,真低相关需换数据源)。
- [2026-08-24] 基本面字段接入(用户拍板,突破单一价量源相关性天花板——双口径体检实测 IC口径灰区42%/PnL口径15%违规后的对策):OSS 有日频 PIT 对齐的 fin_indicators/valuation 表(文档速查表漏记,实探 glob 发现;PIT 验证:ROE 每股每年 ~5 个 distinct 值,跳变精确集中 4/8/10 月披露季)。首批 6 字段 roe/roa/profit_growth/bm/div_yield/ps;**故意排除 PE/PEG**(亏损股负 PE 在 rank 中语义反转)。接入面:loader join+FIELDS+FIELD_DIM(dimless)+FIELD_MEANINGS+3 张机制卡片(cs_value/quality/growth,boost 4.0);算子层零改动。回滚标签 pre-fundamental-20260824。
- [2026-08-24] 冗余清理:alphalab_in/out 中断回测泄漏 313MB(全清);valuation 全列缓存 1.5GB 而只用 3 列 → _ensure_year_cache 加列裁剪(140MB/年→29MB/年);8/17 两个旧 checkpoint 备份删(git 历史已有)。另实测 ps 字段含 inf(营收近零壳公司)且负营收负 PS=负 PE 同款语义反转 → loader 护栏:ps 仅留 >0 且有限,其余字段只挡 inf。
- [2026-08-24] 单层树治理前移(用户指出:不必在失败库下游拒,源头不生成即可)→ ①random_tree 加 min_depth(默认读 REVIEW_MIN_DEPTH,与审查阈值同源,放宽自动跟随);②LLM prompt 硬规则改「深度至少 2 层,顶层算子子节点必须是算子而非裸字段」;③失败模式库清除 14 个单层骨架(它们占 prompt Top-12 名额挤掉有信息量的深层死模式)。单层浪费源:random_tree _depth=1 有 10% 叶概率 + mutate 替换根子树 + LLM 无最小深度约束。
- [2026-08-24] roc 语义闸放宽为「必须包装」(用户指出:库内 Top2 即 rank_cs(roc(rank_cs(log_mv),10)),包装后=排名动量,经济意义清晰)→ 判定改为查 roc 的直接父节点:父为 rank_cs/zscore 放行,裸用于 add/sub/std 仍拒(爆炸真实)。配套:审查端全部结构规则(roc包装/跨量纲/平滑嵌套/极值嵌套/同质退化)静态注入生成 prompt【结构硬规则】——审查规则是确定性语法、零回测信息,静态注入=动态回流且更稳;终审拒因仍走 family_notes(通道在,待首触)。
- [2026-08-24] 终审 0 拒绝排查(用户问"是否太松"):probe 实测 GLM 反而偏严(伪造的均匀逐年收益因"过于规律"被拒;单年依赖规则正确执行)→ 0 拒是漏斗前置的合理结果(到达终审者已过 #2 全年份为正/#7/8/#12 等硬关,盲区候选天然稀少;148 轮仅 47 次调用)。顺手修两隐患:①裁决原文此前全丢弃 → final_review_log.jsonl 审计落盘;②parse_verdict 原 ACCEPT 全文优先,行首 REJECT+后文 ACCEPT 会被误判放行 → 改行首关键词优先+首个出现兜底(fail-open 保持)。
- [2026-08-24] 时区陷阱:Git Bash 的 `TZ=Asia/Shanghai date` 不生效(输出仍是 UTC,差 8 小时)——用 `uv run python -c "from datetime import datetime; print(datetime.now())"` 或 PowerShell Get-Date 查北京时间(Windows 系统时区=中国标准时间,正确)。曾据此误判 16:37 为 08:37,导致一轮被高峰检查跳过后误以为有 bug。
- [2026-08-25] 终审误判根治(4次裁决2次误判,同源:分不清算子参数语义与原子字段边界)→ prompt【字段含义】段加声明:字段均为原子数据列、不得按字段名构成臆测除法(bm≠book÷market)、仅表达式含 div/裸 roc 才评估除零。实测:轮525误判原式改判 ACCEPT,轮517原式拒因从"截断误读"变为合理经济裁量。终审裁决质量链:审计日志(0b4dd12)→行首解析修正→算子语义反例→原子字段声明,四次迭代收敛。
- [2026-08-25] 终审端切换 GLM→DeepSeek v4-pro(用户拍板:GLM 5 裁 2 误判);单次 ~20s 需 DEEPSEEK_TIMEOUT=120(默认 30s 会超时)。配套:终审拒详情(表达式+LLM拒因+IS各项指标含逐年)强制进每轮输出与汇报,误拒候选可走 readmit.py 重审+用户豁免#16。
- [2026-08-26] 10轮批次(526-535)总结:12轮干旱终结,入库5个(含基本面首果 ma(roe,120) 三路合成,OOS零衰减)——破旱三因素:探索模式攒下的搜索面 + 基本面新字段 + DeepSeek 终审(11裁5过3拒0误判,替代GLM的5裁2误)。待裁决:DeepSeek 对 rank_cs(roc(rank_cs)) 包装形态稳定反对(轮526/534两拒),与我们的"包装即放行"规则相left,需用户拍板收紧还是维持。LLM生成质量(过审查率)均值87%为历史最高段。
- [2026-08-26] LLM解析失败攀升根因(3→7→14/轮,防线触发):基本面卡需表达"反向"(ps/bm 低=便宜)但语法无取反手段 → LLM 自造 neg 算子/数字常数 mul(-1,·)、div(1,·)。修法分层:①只禁不管用(仍17%);②给出合法通道「sub(标准化X, 标准化ps) 表达反向组合 + 单字段反向靠引擎自动翻转方向」后定向探针 0/12。教训:LLM 的表达需求堵不住,要给合法出口。
- [2026-08-26] 探索双批次(536-555,20轮全探索+LLM40%)总结:入库10个完成库深度换代(库22,OOS中位0.0551+,新IS王 IC+0.0685/OOS+0.0810),基本面5果(roe×2/ps/roa/bm混血)——混血指引(基本面分支+价量分支各标准化后相加)是基本面字段唯一可行入库形态的判断被完全验证。强探索配置本身有效:LLM过审查率批次均值~93%。遗留观察:OOS多头负因子累积(10入4负),准入#12只查IS,待用户决策。
- [2026-08-27] 第二次干旱停机(轮607,stuck=15):与首次(8/25)不同,这是丰收后exhaustion——两日入库18个换掉20+个后,库内强因子密度高,#9相关性与保优淘劣×1.05门槛大量拦截(相关性高×30+/轮),属"当前字段/结构词汇表下搜索空间充分开采"信号。库况历史最佳:OOS中位+0.0587/负0%/PnL违规48->30。方向:扩字段(income/balance/cashflow三表自算二期)或收割。
- [2026-08-27] 基本面二期扩字段(用户拍板B):income/balance_sheet/cash_flow 三表跨表比率 6 字段(op_margin/asset_turn/ocf_asset/ocf_margin/debt_ratio/np_margin,杜邦+现金流族)+2 张机制卡(cs_dupont/cs_cashflow,boost 6)。PIT 阶梯实测通过(op_margin 每股每年 4 distinct)。README 增「数据字段与机制族」节同步文档。
